# Assistant Bot

This project represents the final assignment for Python learning course

To initialize the project, enter this terminal command int the project root folder
`python -m venv .venv`

## TODO:
 - add project commands description
 - add the instruction of how to set up dependencies and run the project